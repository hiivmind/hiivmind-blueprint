# Gateway Command Template
#
# This template generates the gateway command markdown file.
# Placeholders use {{placeholder}} syntax and are replaced at generation time.
# {{lib_version}} comes from workflow.yaml definitions.source
#
# Execution semantics: https://github.com/hiivmind/hiivmind-blueprint-lib

---
name: {{plugin_name}}
description: >
  Unified entry point for {{plugin_name}} operations - describe what you need
  in natural language or select from the menu.
arguments:
  - name: request
    description: What you want to do (optional - shows menu if omitted)
    required: false
---

# {{plugin_title}} Gateway

Execute this workflow for intelligent routing to the appropriate skill.

> **Workflow:** `${CLAUDE_PLUGIN_ROOT}/commands/{{plugin_name}}/workflow.yaml`
> **Intent Mapping:** `${CLAUDE_PLUGIN_ROOT}/commands/{{plugin_name}}/intent-mapping.yaml`

---

{{#workflow_graph}}
## Workflow Graph Overview

```
{{graph_ascii}}
```
---

## Execution Protocol

**MANDATORY:** This gateway requires loading and following execution semantics from remote sources.

### Phase 1: Initialize

1. Parse command-line arguments and flags
2. Read `workflow.yaml` from `${CLAUDE_PLUGIN_ROOT}/commands/{{plugin_name}}/`
3. Read `intent-mapping.yaml` from same directory

### Phase 2: Load Remote Semantics

**Fetching Protocol (try in order):**
1. gh api (primary): `gh api repos/{owner}/{repo}/contents/{path}?ref={version} --jq '.content' | base64 -d`
2. raw URL (fallback): `https://raw.githubusercontent.com/{owner}/{repo}/{version}/{path}`

**Fetch these files from hiivmind-blueprint-lib@{{lib_version}}:**

| File | Path | Purpose |
|------|------|---------|
| execution-loader.yaml | resolution/execution-loader.yaml | Bootstrap sequence (read first) |
| engine_execution.yaml | execution/engine_execution.yaml | All execution semantics |
| type-loader.yaml | resolution/type-loader.yaml | Type definition loading |
| workflow-loader.yaml | resolution/workflow-loader.yaml | Reference node loading |

### Phase 3: Execute Gateway Workflow

Follow `engine_execution.yaml` traversal section EXACTLY:
1. Initialize state per `execution.state`
2. Start at `start_node` from workflow.yaml
3. Execute each node per `execution.traversal` pseudocode
4. For `reference` nodes, load sub-workflows per `workflow-loader.yaml`
5. **When `invoke_skill` consequence fires**: Use the Skill tool to invoke the matched skill

**The fetched YAML files define execution behavior. Follow them precisely.**

---

## Skill Dispatch Protocol

**CRITICAL:** Gateways are ROUTERS, not executors. When a skill is matched:

1. **DO NOT answer the user's request yourself** - Your job is routing, not answering
2. **DO NOT pre-validate or gather information** - Let the skill handle its own context
3. **IMMEDIATELY invoke the skill** using the Skill tool:

```
Skill(
  skill: "${computed.matched_skill}",
  args: "${arguments}"
)
```

4. **Let the skill take over** - It will load its own SKILL.md and execute its workflow

**Example:** If intent matches "navigate", invoke `{{plugin_name}}-navigate`. Do not search for data yourself.


---


## Usage

```
/{{plugin_name}}                    # Show interactive menu
/{{plugin_name}} [request]          # Route by natural language intent
/{{plugin_name}} --help             # Show full help
/{{plugin_name}} [flags] [request]  # With runtime flags
```

## Quick Examples

{{#examples}}
- `{{command}}` → Routes to {{skill_name}}
{{/examples}}

---

## Runtime Flags

Flags modify workflow execution. Per-invocation only (reset for each command).

| Flag | Effect |
|------|--------|
| `--verbose`, `-v` | Show all node details (level: verbose) |
| `--quiet`, `-q` | Only prompts and final result (level: quiet) |
| `--debug` | Full state dumps (level: debug) |
| `--no-log` | Disable file logging |
| `--no-display` | Silent mode (no terminal output) |
| `--ci` | CI mode: GitHub annotations |

---

## Help Commands

| Command | Description |
|---------|-------------|
| `/{{plugin_name}} --help` | Show full command reference |
| `/{{plugin_name}} -h` | Short form of --help |
| `/{{plugin_name}} help` | Same as --help |
| `/{{plugin_name}} help [skill]` | Help for specific skill |

### Configuration Help

| Command | Description |
|---------|-------------|
| `/{{plugin_name}} help output` | Output levels and configuration |
| `/{{plugin_name}} help prompts` | Prompt modes and match strategies |
| `/{{plugin_name}} help flags` | Quick reference for runtime flags |

---

## Intent Detection

This gateway uses 3VL (3-valued logic) intent detection with **dynamic routing**:

| Value | Meaning | Example |
|-------|---------|---------|
| **T** (True) | Keyword matched | "create" → has_init: T |
| **F** (False) | Negative keyword matched | "don't create" → has_init: F |
| **U** (Unknown) | No match either way | (default state) |

### How Intent Resolution Works

1. **Parse input** against flag keywords → `computed.intent_flags`
2. **Match rules** in priority order → `computed.intent_matches`
3. **Check for clear winner** (2+ point lead)
4. **Dynamic route** to `${computed.matched_action}` → skill execution

This gateway uses O(1) dynamic routing instead of N conditional nodes.

If no clear winner, disambiguation is offered.

### Safety Handling

This gateway includes a standard `error_safety` ending for requests that Claude's built-in safety detects as harmful. The workflow will:

1. Exit cleanly via the `error_safety` ending
2. Display a brief, non-judgmental message
3. Offer recovery guidance

No custom jailbreak detection is implemented—Claude's native safety (95.3% block rate) handles this automatically.


---

## Reference Documentation

- **Type Definitions:** [hiivmind-blueprint-lib](https://github.com/hiivmind/hiivmind-blueprint-lib/tree/{{lib_version}})

---

## Related Skills

{{#skills}}
- {{skill_label}}: `${CLAUDE_PLUGIN_ROOT}/skills/{{skill_directory}}/SKILL.md`
{{/skills}}
