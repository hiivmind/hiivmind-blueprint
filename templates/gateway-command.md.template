# Gateway Command Template
#
# This template generates the gateway command markdown file.
# Placeholders use {{placeholder}} syntax and are replaced at generation time.
#
# See: lib/intent_detection/framework.md for 3VL semantics
# See: lib/workflow/schema.md for workflow structure

---
name: {{plugin_name}}
description: >
  Unified entry point for {{plugin_name}} operations - describe what you need
  in natural language or select from the menu.
arguments:
  - name: request
    description: What you want to do (optional - shows menu if omitted)
    required: false
---

# {{plugin_title}} Gateway

Execute this workflow for intelligent routing to the appropriate skill.

> **Workflow:** `${CLAUDE_PLUGIN_ROOT}/commands/{{plugin_name}}/workflow.yaml`
> **Intent Mapping:** `${CLAUDE_PLUGIN_ROOT}/commands/{{plugin_name}}/intent-mapping.yaml`

---

## Usage

```
/{{plugin_name}}                    # Show interactive menu
/{{plugin_name}} [request]          # Route by natural language intent
```

## Quick Examples

{{#examples}}
- `{{command}}` → Routes to {{skill_name}}
{{/examples}}

---

## Available Operations

{{#skills}}
### {{skill_label}}

{{skill_description}}

**Trigger phrases:** {{trigger_phrases}}

{{/skills}}

---

## Intent Detection

This gateway uses 3VL (3-valued logic) intent detection:

| Value | Meaning | Example |
|-------|---------|---------|
| **T** (True) | Keyword matched | "create" → has_init: T |
| **F** (False) | Negative keyword matched | "don't create" → has_init: F |
| **U** (Unknown) | No match either way | (default state) |

### How Intent Resolution Works

1. **Parse input** against flag keywords
2. **Evaluate rules** in priority order
3. **Find matching rule** where all conditions match
4. **Execute action** (delegate to skill or show menu)

If multiple rules match, the highest priority rule wins.
If no clear winner, disambiguation is offered.

---

## Execution Instructions

### Phase 1: Initialize

1. **Load workflow.yaml** from this command directory:
   Read: `${CLAUDE_PLUGIN_ROOT}/commands/{{plugin_name}}/workflow.yaml`

2. **Load intent-mapping.yaml**:
   Read: `${CLAUDE_PLUGIN_ROOT}/commands/{{plugin_name}}/intent-mapping.yaml`

3. **Initialize runtime state**:
   ```yaml
   workflow_name: {{plugin_name}}-gateway
   current_node: check_arguments
   arguments: "${request}"
   intent: null
   flags:
     has_arguments: ${request != null}
   computed: {}
   ```

---

### Phase 2: Execution Loop

Execute nodes until an ending is reached:

```
LOOP:
  1. Get current node from workflow.nodes[current_node]

  2. Check for ending:
     - IF current_node is in workflow.endings:
       - Display ending.message
       - STOP

  3. Execute by node.type:

     ACTION (parse_intent):
     - Parse user input against intent_flags
     - Evaluate intent_rules in priority order
     - Store matches in computed.intent_matches
     - Route via on_success or on_failure

     CONDITIONAL (check_arguments, check_clear_winner):
     - Evaluate condition
     - Route via branches.true or branches.false

     USER_PROMPT (show_main_menu, show_disambiguation):
     - Present AskUserQuestion
     - Store response, route via handler

     DYNAMIC_ROUTE (execute_matched_intent):
     - Get action from computed.intent_matches.winner
     - Invoke corresponding skill

  4. Record in history and continue

UNTIL ending reached
```

---

## Reference Documentation

- **3VL Framework:** `${CLAUDE_PLUGIN_ROOT}/lib/intent_detection/framework.md`
- **Intent Execution:** `${CLAUDE_PLUGIN_ROOT}/lib/intent_detection/execution.md`
- **Variables:** `${CLAUDE_PLUGIN_ROOT}/lib/intent_detection/variables.md`
- **Workflow Schema:** `${CLAUDE_PLUGIN_ROOT}/lib/workflow/schema.md`

---

## Related Skills

{{#skills}}
- {{skill_label}}: `${CLAUDE_PLUGIN_ROOT}/skills/{{skill_directory}}/SKILL.md`
{{/skills}}
