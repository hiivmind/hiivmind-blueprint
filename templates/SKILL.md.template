# SKILL.md Template
#
# This template generates the SKILL.md that executes a workflow.
# Placeholders use {{placeholder}} syntax and are replaced at generation time.
#
# Execution semantics: .hiivmind/blueprint/execution-guide.md

---
name: {{skill_name}}
description: >
  {{description}}
allowed-tools: {{allowed_tools}}
---

# {{title}}

Execute this workflow deterministically. State persists in conversation context.

---

## Usage

This skill is invoked via the Skill tool:

```
Skill(skill: "{{skill_name}}")
Skill(skill: "{{skill_name}}", args: "[arguments]")
```

Or via gateway routing:
```
/{{parent_plugin_name}} [natural language request]
```

---

## Help

| Command | Description |
|---------|-------------|
| `Skill(skill: "{{skill_name}}", args: "--help")` | Show skill help |
| `Skill(skill: "{{skill_name}}", args: "help")` | Same as --help |

For gateway help: `/{{parent_plugin_name}} help {{skill_short_name}}`

---

{{#if_runtime_flags}}
## Runtime Flags

Flags modify workflow execution. Per-invocation only (reset for each call).

| Flag | Effect |
|------|--------|
| `--verbose`, `-v` | Show all node details (level: verbose) |
| `--quiet`, `-q` | Only prompts and final result (level: quiet) |
| `--debug` | Full state dumps (level: debug) |
| `--no-log` | Disable file logging |
| `--no-display` | Silent mode (no terminal output) |

---
{{/if_runtime_flags}}

{{#examples}}
## Quick Examples

{{#items}}
- `{{invocation}}` → {{outcome}}
{{/items}}

---
{{/examples}}

## Execution

### Step 1: Read Definitions

Read `.hiivmind/blueprint/definitions.yaml` and build the type registry (nodes, consequences, preconditions).

### Step 2: Detect Workflow

```pseudocode
DETECT_PATHS():
  workflow_path ← "${CLAUDE_PLUGIN_ROOT}/skills/{{skill_directory}}/workflow.yaml"
  intent_mapping_path ← "${CLAUDE_PLUGIN_ROOT}/skills/{{skill_directory}}/intent-mapping.yaml"

  IF NOT file_exists(workflow_path):
    FAIL "workflow.yaml is required"
```

### Step 3: Execute Workflow

Follow `.hiivmind/blueprint/execution-guide.md`:
1. **Initialize** — Read workflow.yaml, validate types against definitions, initialize state, check entry preconditions
2. **Execute** — Main loop: dispatch nodes until an ending is reached
3. **Complete** — Display ending message and return result

---

{{#workflow_graph}}
## Workflow Graph Overview

```
{{graph_ascii}}
```

---
{{/workflow_graph}}

{{#if_intent_detection}}
## Intent Detection

This skill uses intent detection when an intent-mapping.yaml is present.

| Value | Meaning | Example |
|-------|---------|---------|
| **T** (True) | Keyword matched | "create" → has_init: T |
| **F** (False) | Negative keyword matched | "don't create" → has_init: F |
| **U** (Unknown) | No match either way | (default state) |

### Resolution Flow

1. **Parse input** against flag keywords → `computed.intent_flags`
2. **Match rules** in priority order → `computed.intent_matches`
3. **Check for clear winner** (2+ point lead)
4. **Route** to matched action or request disambiguation

---
{{/if_intent_detection}}

## Reference Documentation

- **Type Catalog:** [hiivmind-blueprint-lib](https://github.com/hiivmind/hiivmind-blueprint-lib)
- **Authoring Guide:** [hiivmind-blueprint/patterns/authoring-guide.md](https://github.com/hiivmind/hiivmind-blueprint/blob/main/patterns/authoring-guide.md)
- **Execution Guide:** `.hiivmind/blueprint/execution-guide.md`

---

{{#related_skills}}
## Related Skills

{{#skills}}
- {{description}}: `${CLAUDE_PLUGIN_ROOT}/skills/{{skill_id}}/SKILL.md`
{{/skills}}
{{/related_skills}}
